ifeq ($(shell uname), Darwin)
	GCC = gcc-5
else
	GCC = gcc
endif

GCC_FLAGS = -std=c99 -pedantic -Wall -Wextra -Werror -Wfatal-errors -Wno-unused-parameter -Wno-unused-but-set-parameter -Wno-error=pedantic

SHARED_HEADERS = shared
