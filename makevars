GCC_FLAGS = -std=c99 -pedantic -Wall -Wextra -Werror -Wno-error=pedantic -Wfatal-errors -Wno-unused-parameter -Wno-unused-but-set-parameter

ifeq ($(shell uname), Darwin)
	GCC = gcc-5

	AR = libtool -static
	AR_FLAGS = -o
else
	GCC = gcc
	GCC_FLAGS += -D_BSD_SOURCE
	
	AR = ar
	AR_FLAGS = -rvs
endif

SHARED_HEADERS = shared
